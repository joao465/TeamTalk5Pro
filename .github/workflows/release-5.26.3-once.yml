name: Release TeamTalk Pro 5.26.3 Once

on:
  push:
    branches:
      - "teamtalk-pro-mic-eq"
    paths:
      - "RELEASE_5_26_3_TRIGGER.txt"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/publish-pro-release.yml
    with:
      version: "5.26.3"
